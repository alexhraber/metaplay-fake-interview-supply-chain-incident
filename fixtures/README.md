# Public Fixtures

Fixtures are fake or already public:

- `fake-package-json.json`: inert suspicious-looking lifecycle strings;
- `fake-process-env.redacted.json`: fake key names and redacted demo values;
- `fake-stage2-response-summary.json`: behavior summary, not payload code;
- `fake-nohup-runtime-evidence.json`: reconstructed structure for reasoning;
- `iocs.public.json`: indicators already published in `IOCs.md`.

Do not replace these with raw evidence captures.

